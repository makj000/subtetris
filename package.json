{
  "name": "minusfall",
  "version": "2.0.0",
  "description": "Minusfall - a falling-block puzzle where numbered rows clear by subtraction",
  "main": "docs/index.html",
  "scripts": {
    "build": "echo 'No build step required — single HTML file'",
    "cap:sync": "npx cap sync ios",
    "cap:ios": "npx cap open ios",
    "serve": "python3 -m http.server 3456 -d docs"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "keywords": [
    "game",
    "falling-block",
    "html5",
    "canvas"
  ],
  "license": "MIT"
}
